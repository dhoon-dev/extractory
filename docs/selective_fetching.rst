Selective Fetching
==================

Jira callers choose ``fields``, excluded fields, ``expand``, and properties. Gerrit
callers choose repeated ``o=`` options or named presets. Expensive fields, changelog,
comments, messages, files, and full revisions are opt-in.
